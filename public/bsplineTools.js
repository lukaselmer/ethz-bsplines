"use strict";

// you can use: drawLine(ctx, x1, y1, x2, y2);
// you can use: drawLine(ctx, x1, y1, x2, y2);
// you can use: drawCircle(ctx, x, y, radius);

function drawCurve(ctx) {
    // TODO: implement this

}

function drawConstruction(ctx) {
    // TODO: implement this

}

function drawBezierNodes(ctx) {
    // TODO: implement this
}
